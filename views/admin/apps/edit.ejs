<div class="container my-4">
  <div class="row">
    <div class="col-md-8 offset-md-2">
      <div class="card">
        <div class="card-header bg-primary text-white">
          <h4 class="mb-0">Chỉnh sửa ứng dụng</h4>
        </div>
        <div class="card-body">
          <form action="/admin/apps/update/<%= app.id %>" method="POST">
            <input type="hidden" name="_method" value="PUT">
            
            <div class="mb-3">
              <label for="name" class="form-label">Tên ứng dụng <span class="text-danger">*</span></label>
              <input type="text" class="form-control" id="name" name="name" value="<%= app.name %>" required>
            </div>
            
            <div class="mb-3">
              <label for="description" class="form-label">Mô tả</label>
              <textarea class="form-control" id="description" name="description" rows="3"><%= app.description || '' %></textarea>
            </div>
            
            <div class="mb-3">
              <label for="external_app_id" class="form-label">ID ứng dụng bên ngoài</label>
              <input type="text" class="form-control" id="external_app_id" name="external_app_id" value="<%= app.external_app_id || '' %>">
              <small class="text-muted">ID của ứng dụng trên hệ thống bên ngoài (nếu có)</small>
            </div>
            
            <div class="mb-3">
              <label for="external_app_type" class="form-label">Loại ứng dụng bên ngoài</label>
              <input type="text" class="form-control" id="external_app_type" name="external_app_type" value="<%= app.external_app_type || '' %>">
              <small class="text-muted">Loại hoặc nền tảng của ứng dụng bên ngoài (nếu có)</small>
            </div>
            
            <div class="d-flex justify-content-between">
              <a href="/admin/apps" class="btn btn-secondary">Hủy</a>
              <button type="submit" class="btn btn-primary">Cập nhật</button>
            </div>
          </form>
        </div>
      </div>
    </div>
  </div>
</div> 